import Foundation

// MARK: - StrokeTokens

/// Stroke width tokens.
public enum StrokeTokens: Sendable {
    /// 1pt — hairline stroke
    public static let hairline: CGFloat = 1
    /// 2pt — default stroke
    public static let `default`: CGFloat = 2
}
