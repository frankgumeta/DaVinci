# 2.0 release checklist

This checklist is evaluated on one immutable candidate SHA. It is intentionally
kept separate from the local plan, which is excluded from Git.

## Green evidence on the current workspace

- [x] Xcode 27.0 build `27A266a`, Swift 6.4, Swift 6, iOS 17 deployment target.
- [x] Full iOS 27 suite: 702 tests in 75 suites passed.
- [x] Minimum-runtime iOS 17.5 lane passed with snapshots/performance intentionally
      excluded from that OS-specific lane.
- [x] Two clean iOS 27 visual runs: 199 snapshot tests in 21 suites each, zero diffs.
- [x] Product coverage: Tokens 100%, Components 96.70%, Gallery 67.36%; thresholds
      are 100%, 95%, and 60% respectively.
- [x] Performance medians/P95 and Gallery construction smoke tests passed.
- [x] SwiftLint, script tests, package contract, external consumer, API baseline,
      documentation examples, and DocC diagnostics passed.
- [x] Plan directory remains local-only through `.git/info/exclude`.

## Human/repository actions still required

- [ ] Review and approve the candidate SHA in a protected pull request.
- [ ] Run the release workflow from the approved commit; it validates SemVer/tag
      ancestry and creates a draft release with the correct pre-release flag.
- [ ] After approval, publish `v2.0.0` and verify a clean external SPM consumer
      resolves `from: "2.0.0"`.
- [ ] Keep the tag immutable; publish 2.0.1 for critical fixes instead of moving it.
- [ ] Reopen `Unreleased` after publication for compatible 2.0.x work.
